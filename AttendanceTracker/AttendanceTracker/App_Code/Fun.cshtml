
@*
    Her hangi bir View *.cshtm içinde
    @Fun.Format("tarih" , DateTime.Now )
    yazılarak çağrılabilir.
*@

@functions {

    public static string Format( string text, object value ) {
        return String.Format( "{0}:{1}", text, value );
    }

}